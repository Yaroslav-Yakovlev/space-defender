import { Game } from './app/Game.js'
import { CursorHider } from './app/utils.js';

document.addEventListener('DOMContentLoaded', () => {
  const game = new Game()
  const cursorHider = new CursorHider();

})
